function isEven(value) {
  if (value % 2 === 0) {
      return true;
  }
  else {
      return false;
  }
}

// O(1)  -- no matter the input size (value), the algorithm only runs once, thus taking about the same amount of time to complete